# Optional: Custom Polygon RPC URL (uses polygon-rpc.com by default)
# NEXT_PUBLIC_POLYGON_RPC_URL=https://polygon-rpc.com

# Note: Builder signing is handled automatically via Dome's builder-signer service
# No additional credentials needed!
